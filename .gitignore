node_modules/
# Vendored upstream template + lib (regenerated by `stylewright init`)
style-source/
# Per-site working output: dumped vars, screenshots, generated styles
work/
# Local secrets / browser session state
*.storage.json
storage-state*.json
.env
.DS_Store
